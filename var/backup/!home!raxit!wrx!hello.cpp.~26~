#include <bits/stdc++.h>
using namespace std;

void week();
void variable_size();
void printDisplay(vector<int> &v);

int main(int argc, char *argv[]) {
  cout << "Hello Welcome to my Code"
       << "\n";

  int a, b, n;
  const double pi = 3.141'592'653'59;
  double morePrecisePi = 22.0 / 7;

  auto coinFlip = true;
  auto largeNum = 250000000000;

  /*
    cout << "Enter a line of text: ";
    string str;
    getline(cin, str);
  */
  /*
    Start:
    cout << "Enter a number: ";
    cin >> a;
    cout << "Half: " << (a >> 1) << "\n";
    cout << "Double: " << (a << 1) << "\n";
    cout << "Quarter: " << (a >> 2) << "\n";
    cout << "Quadruple: " << (a << 2) << "\n";

    cout << "Do you wish to perform another operation (y/N)? ";
    char repeat;
    cin >> repeat;

    if (repeat == 'y') {
    goto Start;
    }
    cout << "Goodbye!" << "\n";
  */
  vector<int> v;
  cin >> n;
  for (int i = 0; i < n; ++i) {
    cin >> a;
    v.push_back(a);
  }
  // for (int i = 0; i < v.size(); ++i) { //
  //   cout << v[i] << " ";
  // }
  printDisplay(v);
  cout << "\n"
       << "Sorting them in decending order:\n";
  sort(v.begin(), v.end(), [](int Num1, int Num2) { return (Num2 < Num1); });
  printDisplay(v);
  // usin the ternary operator ?:
  // int max = (a > b)? a : b;

  // variable_size();

  return 0;
}
ss
void printDisplay(vector<int> &v) {
  for_each(v.begin(), v.end(), [](int Element) { cout << Element << " "; });
  cout << "\n";
}

void week() {
  enum DaysOfWeek {
    Sunday = 0,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday
  };
  cout << "Find what days of the week are named after!"
       << "\n";
  cout << "Enter a number for a day (Sunday = 0): ";
  int dayInput = Sunday; // Initialize to Sunday
  cin >> dayInput;

  switch (dayInput) {
  case Sunday: {
    cout << "Sunday was named after the Sun"
         << "\n";
    break;
  }
  case Monday: {
    cout << "Monday was named after the Moon"
         << "\n";
    break;
  }
  case Tuesday: {
    cout << "Tuesday was named after Mars"
         << "\n";
    break;
  }
  case Wednesday: {
    cout << "Wednesday was named after Mercury"
         << "\n";
    break;
  }
  case Thursday: {
    cout << "Thursday was named after the Jupitar"
         << "\n";
    break;
  }
  case Friday: {
    cout << "Friday was named after the Venus"
         << "\n";
    break;
  }
  case Saturday: {
    cout << "Saturday was named after the Saturn"
         << "\n";
    break;
  }
  default:
    cout << "Wrong input, execute again"
         << "\n";
    break;
  }
  return;
}

void variable_size() {
  cout << "Computing the size of \
inbuilt variable types"
       << "\n";
  /*
    Just checking.
  */
  int root = 0;
  double pii = 3.14;

  cout << "bool: " << sizeof(bool) << "\n";
  cout << "char: " << sizeof(char) << "\n";
  cout << "unsigned short int: " << sizeof(unsigned short) << "\n";
  cout << "short int: " << sizeof(short) << "\n";
  cout << "unsigned long int: " << sizeof(unsigned long) << "\n";
  cout << "long: " << sizeof(long) << "\n";
  cout << "int: " << sizeof(int) << "\n";
  cout << "uns. long long: " << sizeof(unsigned long long) << "\n";
  cout << "long long: " << sizeof(long long) << "\n";
  cout << "unsigned int: " << sizeof(unsigned int) << "\n";
  cout << "float: " << sizeof(float) << "\n";
  cout << "double: " << sizeof(double) << "\n";

  cout << "The output changes with compiler, hardware and OS"
       << "\n";
  return;
}
