# -*- mode: snippet -*-
# name: prime-check-fun
# contributor: Likhon
# key: /prime
# --
bool isPrime (int n)$0 {
  if (n <= 1) return false;

  for (int i = 3; (i * i) <= n; i += 2) {
    if (!(n%i))
      return false;
  }
  return true;
}
